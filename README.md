# 👋 Hey, I'm Vikash Vishwakarma  

I'm a B.Tech 3rd-year student who’s pretty hooked on how **Artificial Intelligence** actually works —  
not just the buzzword type, but the real math and logic behind it.  

I’m not claiming to be a pro yet, but every project I build teaches me something new —  
and that’s what keeps me going.  

---

### 🌱 What I’m up to these days
- Learning **Deep Learning** and **Data Science** (slowly, but consistently 😅)  
- Trying to build small AI models and visualize how they think  
- Reading about real-world ML applications and where they actually make sense  
- Cleaning messy datasets (because someone has to 😆)  

---

### ⚙️ Tools I use the most
- **Languages:** Python, C++, Java  
- **Libraries:** NumPy, Pandas, TensorFlow, Matplotlib, Scikit-learn  
- **Web:** HTML, CSS, Flask  
- **Database:** MySQL, MongoDB  
- **Others:** Git, GitHub, VS Code  

---

### 🚀 Goals (that I actually care about)
- Build something that genuinely helps someone, not just a college project  
- Deploy at least one working ML model online  
- Get better at writing clean, readable code  
- Maybe start contributing to open-source once I stop breaking my own code 😅  

---

### 📫 Reach me here
- 📧 [vikasvish2226@gmail.com](mailto:vikasvish2226@gmail.com)  
- 🧑‍💻 [github.com/vikasvish2226](https://github.com/vikasvish2226)  

---

> “Every expert was once a beginner. I’m just trying to be a better beginner every day.”  

---

## 📊 My GitHub Stats  

![Vikas's GitHub stats](https://github-readme-stats.vercel.app/api?username=vikasvish2226&show_icons=true&theme=transparent&hide_border=true)

## 🧩 Languages I mess with the most  

![Top Langs](https://github-readme-stats.vercel.app/api/top-langs/?username=vikasvish2226&layout=compact&theme=transparent&hide_border=true)

---

![Profile views](https://komarev.com/ghpvc/?username=vikasvish2226&label=Profile%20views&color=gray&style=flat)

## Local Development

1. Copy `.env.example` to `.env` and set `MONGODB_URI`.
2. Start the API with `npm run server`.
3. Start the frontend with `npm run dev`.

The local frontend uses `/api` through the Vite proxy at `http://localhost:5000`.

## Production Deployment

Deploy the frontend and backend separately. The frontend must use a build-time `VITE_API_URL` containing the public backend URL, for example `https://api.example.com/api`. For the GitHub Pages workflow, add a repository secret named `VITE_API_URL`; the workflow passes it into the production build so the website never posts registration data to GitHub Pages itself. The backend must define `MONGODB_URI`, `PORT`, and `FRONTEND_URL` in its hosting provider's environment settings. `FRONTEND_URL` may contain comma-separated frontend origins when more than one is required.

### Render Backend Deployment

This repository includes `render.yaml` for the existing Express backend. In Render, choose **New > Blueprint**, connect this GitHub repository, and select the `main` branch. Render will use the `backend/` server through the root `npm start` command and expose `GET /api/health` as its health check.

Set these Render environment variables:

```text
MONGODB_URI=mongodb+srv://<atlas-user>:<atlas-password>@<atlas-host>/restaurantQR
FRONTEND_URL=https://vikasvish2226.github.io
PORT=10000
```

After Render deploys, copy its generated URL, for example `https://qr-kitchen-api.onrender.com`. The exact URL is generated by Render and must not be guessed. Verify `https://<render-url>/api/health` returns JSON with `status: "ok"`.

Then add this GitHub Actions repository secret:

```text
VITE_API_URL=https://<render-url>/api
```

Push or manually run the Pages workflow again. The deployed frontend registration request is `POST https://<render-url>/api/users`.

Never commit `.env`, MongoDB credentials, or other secrets. GitHub Pages can host the static frontend only; MongoDB access remains server-side through the Express API.
